# Session 3 — 2026-07-08 (laptop, Claude Code — Phase 3.5: agent access model)

## What was done
- First Claude Code session; both repos cloned to laptop at
  C:\Users\jm2_c\HomeInfra\ (git auth via Git Credential Manager)
- Backfilled ADR-003 (n8n placement), ADR-004 (no ERP monolith),
  ADR-005 (backup immutability) — decided in session 2, never committed
- New ADR-006: coding agents (Claude Code/Codex) get no standing
  identity — they borrow Christian's supervised session + ssh-agent;
  agent_ops stays the observation identity; svc-claude token at Phase 6
- New ADR-007: Hermes (kept in plan, confirmed by Christian) runs on the
  NAS tailnet-only with zero direct capability, gateway-mediated only
- Rewrote scripts/refresh-state.sh:
  - NAS section now connects as agent_ops@nas and uses commands that
    exactly match the sudo allowlist (bare `docker network ls` — the
    rule has no wildcard; `docker ps *` allows --format)
  - VPS section runs locally when on the VPS (detects /opt/vps-infra),
    over `ssh vps` otherwise
  - set -Eeuo pipefail, temp-dir staging, non-empty check before
    replacing state files, previous good snapshot preserved on failure
- Created homelab-infra/docs/agent-implementation-plan.md (CLAUDE.md
  pointed to it but it never existed): full roadmap, Phase 3.5 marked
  done, new Phase 4.5 = Home Assistant on NAS (HA Container, host
  network, /volume1/appdata/homeassistant, native auth, tailnet/LAN
  only; independent of Phase 4)

## Decisions
- state/ snapshots WILL be committed (private repo, no secrets in
  output, gives infra drift history)
- Home Assistant added to roadmap as Phase 4.5 on the NAS — the VPS
  can't see the home LAN (mDNS/USB dongles), so NAS is the only option

## Loose ends
- VPS clone of this repo has uncommitted refresh-state.sh changes and a
  scripts/refresh-state.sh.backup file from the aborted ChatGPT session.
  On next VPS session: `git checkout scripts/refresh-state.sh`, delete
  the .backup, `git pull`.
- `ssh agent_ops@nas` from laptop/VPS not yet verified — Tailscale SSH
  ACLs may need to permit login as agent_ops, or agent_ops pubkey needs
  distributing. Verify before relying on refresh-state.sh NAS section.
- refresh-state.sh not run this session (agent_ops access unverified);
  no state/ files exist in the repo yet
- Stale DNS records (pretix, chat) and stale VPS docker networks
  (universal-capture_*) still pending cleanup

## What's next
- Verify agent_ops SSH path, run refresh-state.sh, commit first state/
- Phase 4 (n8n on NAS) or Phase 4.5 (Home Assistant) — Christian's pick
