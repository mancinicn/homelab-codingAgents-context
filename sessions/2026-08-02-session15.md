# Session 15 — 2026-08-02 (Android Termux — access setup and backlog verification)

## Termux access established

Configured and verified Christian's Android Termux environment for supervised homelab work:

- GitHub SSH authentication works as `mancinicn`.
- `ssh vps` reaches `srv970661` as `mancinicn`.
- `ssh agent_ops@nas` reaches `DXP2800-702E` as restricted `agent_ops`.
- Termux reuses a persistent `ssh-agent`; keys remain passphrase-protected.
- Both repositories were confirmed clean and aligned before work began.
- Live-state refresh completed and commit `3be0482` recorded the 2026-08-02 snapshot.

## Authentik verification

A read-only query of the live Authentik database returned these active users:

- `Birte`
- `admin`
- `akadmin`
- `mancinicn`
- `AnonymousUser`
- two `ak-outpost-*` service accounts

### Backlog correction

- Wife's Authentik account: **completed**. `Birte` exists and is active.
- `akadmin` cleanup: **still open**. Do not disable it until another human account is confirmed to have full working administrative access.

## Previously completed items confirmed

- Hermes Telegram chat is already live and verified end-to-end.
- Main Authentik upgrade is complete.
- Vaultwarden upgrade is complete.
- Immich is installed and healthy, but account onboarding still requires verification.

## Verification still pending

1. VPS notification configuration.
2. Hermes Home Assistant token.
3. Hermes Watchdog scheduling status.
4. Immich onboarding and user count.
5. Vaultwarden Category-B credential recovery, checked manually inside the vault.

No secrets were written to the repository or chat.
