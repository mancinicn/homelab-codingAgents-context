# Session 15 — 2026-08-02
## Android Termux access and backlog verification

## Termux access established

Configured and verified Christian's Android Termux environment for supervised homelab work:

- GitHub SSH authentication works as `mancinicn`.
- `ssh vps` reaches `srv970661` as `mancinicn`.
- `ssh nas` reaches `DXP2800-702E` as restricted `agent_ops`.
- Direct NAS administration works as `mancinicn` through Tailscale SSH.
- Termux reuses a persistent `ssh-agent`; private keys remain passphrase-protected.
- Both repositories were clean and aligned at session start.
- Live NAS/VPS state was refreshed and committed as `3be0482`.

## Authentik verification

Live users confirmed:

- `Birte` — active, member of `family`
- `mancinicn` — active, member of `family`
- `admin` — active, member of `authentik Admins`, superuser
- `akadmin` — active, member of `authentik Admins`, superuser
- `AnonymousUser` — active internal account
- two active `ak-outpost-*` service accounts

Administrator activity:

- `admin` last login: 2026-07-26 13:58 UTC
- `akadmin` last login: 2026-07-12 15:11 UTC
- `mancinicn` has never logged in directly

Backlog status:

- Wife's Authentik account: **complete**
- `admin` appears to be the intended replacement administrator
- `akadmin` cleanup: **still open**
- A fresh interactive `admin` login and admin-interface test is required before disabling `akadmin`

## Hermes

### Telegram chat

- Hermes conversational Telegram chat was already verified working end-to-end on 2026-07-17.

### Watchdog

- The n8n workflow `Hermes Watchdog Trigger` exists.
- Its live `active` value is `true`.
- Hermes Watchdog is therefore installed, active and scheduled.

### Home Assistant integration

- `HA_TOKEN` exists in `/etc/nas-secrets/hermes.env`.
- The same token is loaded into the running Hermes container.
- Both copies receive HTTP 401 from the Home Assistant API.
- This rules out stale container configuration; the token itself is invalid, revoked or corrupted.

Active Home Assistant users:

- `Home Assistant Content` — active, non-owner, `system-read-only`
- `mancinicn` — active owner, `system-admin`
- `Home Assistant Cloud` — active non-owner, `system-admin`
- `Birte` — active non-owner, `system-users`

No explicitly named Hermes service account exists.

Remaining decision:

- For read-only access, `Home Assistant Content` has an appropriate restricted role.
- Hermes device-control functionality would require a non-admin account with sufficient control permissions.
- Generate and install a new token only after confirming the intended permission scope.

## Immich

Live database verification found one active user:

- `Christian N Mancini`
- administrator: yes
- account status: active
- `shouldChangePassword=true`

Backlog status:

- Immich admin onboarding: **complete**
- Initial administrator password change: **still open**
- Separate Immich account for Birte: **not created**

## VPS notifications

- `/etc/vps-secrets/notify.env` does not exist.
- VPS Telegram failure notifications are therefore **not configured**.

## VPS maintenance observation

The VPS reported:

- 67 available package updates
- one additional ESM Apps security update
- one unattended update failure
- system restart required

No package update or reboot was performed during this session.

## Remaining verified backlog

1. Create `/etc/vps-secrets/notify.env` and test VPS failure notifications.
2. Replace the invalid Hermes Home Assistant token after deciding the intended account permissions.
3. Complete the Immich administrator's initial password change.
4. Create Birte's Immich account.
5. Verify an interactive Authentik login as `admin`, then disable—not delete—`akadmin`.
6. Verify Vaultwarden Category-B credential recovery inside the unlocked vault.
7. Review and apply VPS operating-system updates under the project's supervised reboot procedure.

## Deliberately deferred

- Immich import of legacy `/volume1/Photos`
- Mobile operations dashboard
- Replacement of the old B2 key
- Permanent fix for NAS container survival issues after reboot

No secrets were printed, committed or written to chat.
