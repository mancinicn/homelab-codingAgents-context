# Session 5 — 2026-07-12 (laptop, Claude Code — Phase 5: backup gateway)

## What was done
- Deployed the append-only backup gateway on the VPS per ADR-010:
  `rclone/rclone:1.74.4` running `serve restic --append-only` against
  a new, purpose-scoped B2 application key (listFiles+readFiles+
  writeFiles, no deleteFiles), bound to the VPS's Tailscale IP only
  (100.94.111.98:8200)
- Rough credential-entry process (worth noting for next time, not a
  design problem): lost the first B2 Application Key before saving it
  anywhere (Backblaze only shows it once) — recreated the key. Then a
  gateway-auth password (GATEWAY_USER/GATEWAY_PASS, generated locally
  by the save script, unrelated to B2) also got lost/mistyped once —
  reset via a small dedicated script that only touches those two lines,
  leaving the already-working B2 config untouched
- **Verified both required properties before touching any live config:**
  1. Visibility: `restic snapshots` through the gateway correctly lists
     all 8 existing snapshots (smoke-test + 7 days of real photo
     backups, 2026-07-06 through 07-11) — confirms the gateway serves
     the real existing repository, not an empty/different one
  2. Append-only enforcement: `restic forget` on the disposable
     smoke-test snapshot was rejected with repeated `403 Forbidden`
     from the gateway — confirms deletion is genuinely blocked, not
     just documented as a policy. Real photo snapshots were never
     targeted for this test, by design (used the pre-existing
     smoke-test snapshot specifically because it's disposable)

## Continued same session — cutover completed, Phase 5 CLOSED

## What was done
- Christian approved the cutover. New script
  `nas/scripts/cutover-to-backup-gateway.sh`:
  - Backs up the current `/home/mancinicn/.config/restic/b2.env` to a
    timestamped copy first (`b2.env.pre-gateway-20260712-115341`) —
    instantly revertible with a single `cp` if anything had gone wrong
  - Rewrites it to point `RESTIC_REPOSITORY` at the gateway
    (`rest:http://nas-backup:...@100.94.111.98:8200/`), drops the now-
    unused `AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY` (NAS no longer
    talks to B2 directly at all — old credentials still safe in the
    backup file, not lost)
  - `RESTIC_PASSWORD_FILE` left untouched — same encryption password,
    same repository contents, only the transport/auth changed
  - This file is owned by `mancinicn` (not root), so ran directly via
    normal SSH access, no sudo/Christian-interactive-password step needed
- Ran the actual backup script directly (`restic-photos-backup.sh`,
  the exact thing the systemd timer calls) rather than waiting for
  tomorrow's 3am run: completed successfully, `snapshot 796d6118 saved`
- Verified full continuity: `restic snapshots` shows all 9 snapshots
  (8 historical + the new one), correct host/tags/paths throughout —
  nothing lost, nothing duplicated, new write landed correctly
- Tomorrow's 3am scheduled run needs no separate changes — same script,
  same (now-updated) config file, will automatically go through the
  gateway

## Phase 5 status: DONE
The hard gate referenced by invariant #4 / ADR-006 / ADR-007 is now
satisfied: the NAS cannot delete existing backups even if fully
compromised (enforced by both the B2 key's capabilities and the
gateway's own append-only logic, independently). This was the
condition for any future agent write/destructive infrastructure
capability.

## Loose ends
- Old full-delete-capability B2 key still exists in Backblaze (now
  unused by the NAS, only in the local backup env file) — decide
  whether to delete/rotate it in Backblaze itself
- Local backup file on NAS (`b2.env.pre-gateway-20260712-115341`)
  contains the old B2 credentials in plaintext — fine short-term (same
  600 permissions, same host), but worth deleting once Christian's
  confident the cutover is stable (give it a few scheduled runs first)
- akadmin active-vs-disabled discrepancy — still open from last session
- Stray expired `authtokenOutpost` Authentik token — still not cleaned
  up (harmless)
- Wife's Tailscale + n8n invites — sent, acceptance not yet confirmed
- Mobile dashboard for viewing containers (Dozzle vs Portainer) —
  discussed, parked, not decided

## What's next
- Phase 6 — ops gateway v1 (read-only) is the next real structural
  milestone: named read-only actions (service_status, get_logs,
  disk_usage, backup_health) via scoped bearer tokens for agents,
  on the NAS, tailnet-only
- Or: smaller loose ends above (akadmin, invite confirmations, old B2
  key decision, mobile dashboard) if Christian wants to clear those first
